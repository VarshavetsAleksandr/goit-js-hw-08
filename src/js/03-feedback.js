const form = document.querySelector('.feedback-form');
const LOCALSTORAGE_KEY = 'feedback-form-state';

// form.addEventListener('input', handlerInput);

// function handlerInput() {}
console.dir(form.elements.message.value);
console.dir(form);
